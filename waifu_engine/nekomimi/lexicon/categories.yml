# Category policy sets. Skip-on-yes and pin order stay in engine.py.
# series_appearance flags mark the angel kit (halo, wings, horns). hair_color
# is prepended by the loader. A series lock does not pin that set. Hair and
# the kit are pinned only when player text named them and enrich.yml has not
# already recorded the answer. series_split flags are rare looks the engine
# pins when they divide the locked cast.
# popular titles feed sources.popular_characters. Runtime never reads catalog.json.
categories:
  medium: { broad: true }
  gender: { broad: true }
  meta: { broad: true }
  hair_color: { color_detail: true }
  hair: { color_detail: true }
  look_halo: { series_appearance: true }
  look_wings: { series_appearance: true }
  look_horns: { series_appearance: true }
  look_prosthetic: { series_split: true }
  look_ears: { series_split: true }
  look_eyepatch: { series_split: true }
  look_tail: { series_split: true }
popular:
  game:
    - "Category:Female characters in video games"
    - "Category:Male characters in video games"
  comic:
    - "Category:Marvel Comics superheroes"
    - "Category:DC Comics superheroes"
  movie:
    - "Category:Female characters in film"
    - "Category:Male characters in film"
  tv:
    - "Category:Female characters in television"
    - "Category:Male characters in television"
